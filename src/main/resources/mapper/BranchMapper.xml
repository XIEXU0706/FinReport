<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.finreport.module.business.mapper.BranchMapper">

    <select id="selectById" resultType="org.example.finreport.module.business.entity.Branch">
        SELECT * FROM branch WHERE id = #{id}
    </select>

    <select id="selectList" resultType="org.example.finreport.module.business.entity.Branch">
        SELECT * FROM branch
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO branch (branch_code, branch_name, region, status, created_at)
        VALUES (#{branchCode}, #{branchName}, #{region}, #{status}, NOW())
    </insert>

    <update id="updateById">
        UPDATE branch
        <set>
            <if test="branchCode != null">branch_code = #{branchCode},</if>
            <if test="branchName != null">branch_name = #{branchName},</if>
            <if test="region != null">region = #{region},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM branch WHERE id = #{id}
    </delete>

    <select id="selectPage" resultType="org.example.finreport.module.business.entity.Branch">
        SELECT * FROM branch
        <where>
            <if test="branchName != null and branchName != ''">AND branch_name LIKE CONCAT('%',#{branchName},'%')</if>
            <if test="region != null and region != ''">AND region = #{region}</if>
            <if test="status != null and status != ''">AND status = #{status}</if>
        </where>
        ORDER BY id DESC LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="selectCount" resultType="long">
        SELECT COUNT(*) FROM branch
        <where>
            <if test="branchName != null and branchName != ''">AND branch_name LIKE CONCAT('%',#{branchName},'%')</if>
            <if test="region != null and region != ''">AND region = #{region}</if>
            <if test="status != null and status != ''">AND status = #{status}</if>
        </where>
    </select>

    <select id="selectMaxBranchCode" resultType="string">
        SELECT branch_code FROM branch ORDER BY id DESC LIMIT 1
    </select>

</mapper>
