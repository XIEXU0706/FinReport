<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.finreport.module.system.mapper.SysMenuMapper">

    <select id="selectById" resultType="org.example.finreport.module.system.entity.SysMenu">
        SELECT * FROM sys_menu WHERE id = #{id}
    </select>

    <select id="selectList" resultType="org.example.finreport.module.system.entity.SysMenu">
        SELECT * FROM sys_menu
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_menu (menu_name, parent_id, sort_order, path, component, menu_type, visible, icon, perms, created_at)
        VALUES (#{menuName}, #{parentId}, #{sortOrder}, #{path}, #{component}, #{menuType}, #{visible}, #{icon}, #{perms}, NOW())
    </insert>

    <update id="updateById">
        UPDATE sys_menu
        <set>
            <if test="menuName != null">menu_name = #{menuName},</if>
            <if test="parentId != null">parent_id = #{parentId},</if>
            <if test="sortOrder != null">sort_order = #{sortOrder},</if>
            <if test="path != null">path = #{path},</if>
            <if test="component != null">component = #{component},</if>
            <if test="menuType != null">menu_type = #{menuType},</if>
            <if test="visible != null">visible = #{visible},</if>
            <if test="icon != null">icon = #{icon},</if>
            <if test="perms != null">perms = #{perms},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM sys_menu WHERE id = #{id}
    </delete>

</mapper>
