# ============================================================
# 本地环境凭证模板
# 复制本文件为 credentials.yml，填入你的实际值
# cp config/credentials.example.yml config/credentials.yml
# ============================================================

# MySQL 数据库
DB_PASSWORD:
DB_HOST: localhost
DB_USERNAME: root

# 阿里云 OSS（如不使用可留空）
OSS_ACCESS_KEY_ID:
OSS_ACCESS_KEY_SECRET:
OSS_ENDPOINT: https://oss-cn-hangzhou.aliyuncs.com
OSS_BUCKET_NAME:
